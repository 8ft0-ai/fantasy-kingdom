let scarMarkerGroup=null;
function scarMarkerMesh(scar){let group=new THREE.Group(),state=scar.state,type=scar.type,baseColor=type==='battlefield'?0x6f5a49:type==='fire-scar'?0x4a3025:type==='plague-marker'?0x5d6b45:type==='omen-site'?0x66517a:0x77706a,base=new THREE.Mesh(new THREE.CylinderBufferGeometry(20,26,8,8),getMat(`scar-${type}`,baseColor));base.position.y=4;group.add(addPick(base,{kind:'scar',scar}));if(state==='rebuilding'){let frame=new THREE.Mesh(new THREE.BoxBufferGeometry(38,28,38),getMat('scar-frame',0xc4a66a));frame.position.y=18;group.add(addPick(frame,{kind:'scar',scar}))}else if(state==='memorial'){let stone=new THREE.Mesh(new THREE.ConeBufferGeometry(13,58,5),getMat('scar-memorial',0xb4aa92));stone.position.y=29;group.add(addPick(stone,{kind:'scar',scar}))}else{let mark=new THREE.Mesh(type==='battlefield'?new THREE.ConeBufferGeometry(18,32,4):new THREE.TorusBufferGeometry(22,5,6,16),getMat(`scar-mark-${type}`,type==='fire-scar'?0x2a1712:0x8b7b63));mark.position.y=type==='battlefield'?16:14;if(type!=='battlefield')mark.rotation.x=Math.PI/2;group.add(addPick(mark,{kind:'scar',scar}))}group.position.set(scar.x,scar.y+5,scar.z);return group}
function refreshScarMarkers(){if(!scene||typeof THREE==='undefined')return;if(scarMarkerGroup)scene.remove(scarMarkerGroup);scarMarkerGroup=new THREE.Group();for(const scar of scarState().registry)if(!scar.erased)scarMarkerGroup.add(scarMarkerMesh(scar));scene.add(scarMarkerGroup)}
const scarBaseSetupScene=setupScene,scarBaseMove=move;
setupScene=function(){scarBaseSetupScene();refreshScarMarkers()};
move=function(dt){scarBaseMove(dt);if(scarMarkerGroup)scarMarkerGroup.visible=camera.position.y<2600};
