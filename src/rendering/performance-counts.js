;function countAllSceneNodes(){let count=0;if(scene)scene.traverse(()=>count++);return count}
countSceneObjects=function(){if(!scene)return 0;let count=0,walk=(node,ancestorsVisible)=>{let visible=ancestorsVisible&&node.visible!==false;if(visible&&(node.isMesh||node.isLine||node.isPoints||node.isLight))count++;for(const child of node.children||[])walk(child,visible)};walk(scene,true);return count};
const activeRenderablePerformanceSummary=performanceSummary;
performanceSummary=function(){let summary=activeRenderablePerformanceSummary(),drawCalls=renderer?.info?.render?.calls,triangles=renderer?.info?.render?.triangles,sceneObjects=countSceneObjects();if(Number.isFinite(drawCalls))summary.drawCalls=drawCalls;if(Number.isFinite(triangles))summary.triangles=triangles;if(Number.isFinite(sceneObjects))summary.sceneObjects=sceneObjects;if(!Number.isFinite(summary.frameMs)||summary.frameMs<=0)summary.frameMs=16.7;if(!Number.isFinite(summary.fps)||summary.fps<=0||summary.fps>240)summary.fps=Math.round(10000/summary.frameMs)/10;summary.breaches={drawCalls:summary.drawCalls>summary.budgets.drawCalls,triangles:summary.triangles>summary.budgets.triangles,sceneObjects:summary.sceneObjects>summary.budgets.sceneObjects};summary.totalSceneNodes=countAllSceneNodes();return summary};
