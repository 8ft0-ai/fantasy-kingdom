;function setPerformanceAuto(){let state=performanceState();state.forced=false;setPerformanceQuality(inferredPerformanceQuality(),false,'automatic device profile');return state.quality}
function performanceControls(){let s=performanceSummary();return`<div class="section"><b>Performance</b><p class="small">Quality <b>${safeName(s.quality)}</b>${s.forced?' · fixed':' · adaptive'}. The governor lowers scene cost when frame rate or rendering budgets are exceeded.</p><div class="grid"><button onclick="setPerformanceAuto()">Auto</button><button onclick="setPerformanceQuality('high')">High</button><button onclick="setPerformanceQuality('balanced')">Balanced</button><button onclick="setPerformanceQuality('low')">Low</button><button onclick="setPerformanceQuality('minimal')">Minimal</button><button onclick="togglePerformanceMeter()">Metrics</button></div><div class="kv"><b>Frame rate</b><span>${s.fps.toFixed(1)} fps</span><b>Draw calls</b><span>${s.drawCalls} / ${s.budgets.drawCalls}</span><b>Triangles</b><span>${s.triangles.toLocaleString()} / ${s.budgets.triangles.toLocaleString()}</span><b>Scene objects</b><span>${s.sceneObjects} / ${s.budgets.sceneObjects}</span><b>Object growth</b><span>${s.maxObjectGrowth}</span></div></div>`}
const performanceBaseWorldPanel=worldPanel;
worldPanel=function(){return performanceBaseWorldPanel()+performanceControls()};
const performanceBaseHud=hud;
hud=function(){performanceBaseHud();let s=performanceState();meta.innerHTML+=`<span class="chip">Quality ${safeName(s.quality)}</span>`};
if(typeof window!=='undefined'){window.setPerformanceAuto=setPerformanceAuto;window.togglePerformanceMeter=togglePerformanceMeter;window.setPerformanceQuality=setPerformanceQuality}