;const acceptanceRequestedAtLoad=typeof location!=='undefined'&&location.hash.includes('accept=1'),acceptanceRequestedSeed=typeof location!=='undefined'?(location.hash.match(/s=([^&]+)/)||[])[1]||'1234567':'1234567';
const acceptanceBaseSetDrawerTab=setDrawerTab,acceptanceBaseDrawer=drawer;
setDrawerTab=function(tab){if(tab==='acceptance'){productUiState().tab='acceptance';drawer();return}acceptanceBaseSetDrawerTab(tab)};
function acceptanceTabs(){return`<div class="ui-tabs">${productTabButton('time','Time')}${productTabButton('rates','Rates')}${productTabButton('acts','Acts')}${productTabButton('world','World')}${productTabButton('houses','Houses')}${productTabButton('overlays','Overlays')}${productTabButton('acceptance','Test')}</div>`}
function acceptanceMarkHtml(value){return`<span class="acceptance-mark ${value?'pass':'pending'}">${value?'PASS':'PENDING'}</span>`}
function acceptanceCheckRows(checks){return Object.entries(checks||{}).map(([key,value])=>`<b>${safeName(key.replace(/([A-Z])/g,' $1'))}</b><span>${acceptanceMarkHtml(!!value)}</span>`).join('')}
function acceptancePanel(){let result=window.ANNALS_ACCEPTANCE_RESULT||acceptanceSummary(),running=!!window.ANNALS_ACCEPTANCE_RUNNING,five=result.fiveMinute||{},thirty=result.thirtyMinute||{},seeds=result.knownSeeds||ACCEPTANCE_REGRESSION_SEEDS;return`<div class="section"><b>Acceptance harness</b><p class="small">The five-minute and 30-minute windows use the default pace of eight simulated days per second: ${ACCEPTANCE_FIVE_MINUTE_DAYS.toLocaleString()} and ${ACCEPTANCE_THIRTY_MINUTE_DAYS.toLocaleString()} days.</p>${running?'<p><b>Running accelerated acceptance…</b></p>':''}<div class="kv"><b>Seed</b><span>${safeName(result.seed||world.seed)}</span><b>Five-minute test</b><span>${acceptanceMarkHtml(!!five.passed)}</span><b>30-minute test</b><span>${acceptanceMarkHtml(!!thirty.passed)}</span><b>Elapsed</b><span>${Math.max(0,Math.floor((result.currentDay||clock.day)-(result.startedDay||1))).toLocaleString()} days</span></div></div><div class="section"><b>Five-minute checks</b><div class="kv">${acceptanceCheckRows(five.checks)}</div><p class="small">Chronicle entries ${five.evidence?.chronicleEntries??0} · named events ${five.evidence?.namedCharacterEvents??0} · incidents ${five.evidence?.incidents??0} · buildings added ${five.evidence?.buildingsAdded??0}</p></div><div class="section"><b>30-minute checks</b><div class="kv">${acceptanceCheckRows(thirty.checks)}</div><p class="small">Successions ${thirty.evidence?.successions??0} · wars ${thirty.evidence?.wars??0} · battles ${thirty.evidence?.battles??0} · peace ${thirty.evidence?.peace??0} · disasters ${thirty.evidence?.disasters??0}</p></div><div class="section"><b>Regression seeds</b><p class="small">Each opens an isolated accelerated run so the visible realm is not replaced.</p><div class="grid">${seeds.map(seed=>`<a class="acceptance-button" target="_blank" rel="noopener" href="${location.pathname}#s=${encodeURIComponent(seed)}&debug=1&accept=1">Test ${safeName(seed)}</a>`).join('')}</div></div>${window.ANNALS_ACCEPTANCE_RESULT?`<div class="section"><b>Last accelerated run</b><div class="kv"><b>Overall</b><span>${acceptanceMarkHtml(!!result.passed)}</span><b>Duration</b><span>${Number(result.durationMs||0).toLocaleString()} ms</span><b>Generation</b><span>${Number(result.generation?.durationMs||0).toLocaleString()} ms</span></div></div>`:''}`}
drawer=function(){let panel=document.getElementById('drawer');if(productUiState().tab==='acceptance'){panel.innerHTML=`<b>ANNALS ${safeName(world.version)}</b>${acceptanceTabs()}${acceptancePanel()}<p class="small">Hotkeys: Space pause · 1–4 speed · H hide UI · C Watch · Esc deselect.</p>`;return}acceptanceBaseDrawer();let tabs=panel.querySelector('.ui-tabs');if(tabs&&!tabs.querySelector('[data-acceptance-tab]'))tabs.insertAdjacentHTML('beforeend',`<button data-acceptance-tab onclick="setDrawerTab('acceptance')">Test</button>`)};
function runAcceptanceInPlace(seed=world.seed){window.ANNALS_ACCEPTANCE_RUNNING=true;productUiState().tab='acceptance';drawer();setTimeout(()=>{let result=runAcceptanceSuite(String(seed),ACCEPTANCE_THIRTY_MINUTE_DAYS);window.ANNALS_ACCEPTANCE_RESULT=result;window.ANNALS_ACCEPTANCE_RUNNING=false;productUiState().tab='acceptance';drawer()},30)}
if(typeof document!=='undefined'){let style=document.createElement('style');style.textContent='.acceptance-mark{font-size:10px;letter-spacing:.08em;padding:2px 5px;border:1px solid #806f55;border-radius:3px}.acceptance-mark.pass{background:#304c32;color:#e9f0db}.acceptance-mark.pending{background:#514535;color:#eadcc0}.acceptance-button{display:inline-block;padding:5px 7px;border:1px solid #6d5a3d;border-radius:3px;background:#2f271d;color:#eadcc0;text-decoration:none;text-align:center}.acceptance-button:hover{background:#493b29}';document.head.appendChild(style)}
if(typeof window!=='undefined'){window.setDrawerTab=setDrawerTab;window.runAcceptanceInPlace=runAcceptanceInPlace;addEventListener('load',()=>{startAcceptanceTracking();if(window.ANNALS_DEBUG)Object.assign(window.ANNALS_DEBUG,{acceptanceSummary,runAcceptance:runAcceptanceSuite,acceptanceSeeds:[...ACCEPTANCE_REGRESSION_SEEDS],acceptanceWindows:{fiveMinuteDays:ACCEPTANCE_FIVE_MINUTE_DAYS,thirtyMinuteDays:ACCEPTANCE_THIRTY_MINUTE_DAYS}});if(acceptanceRequestedAtLoad){productUiState().tab='acceptance';drawer();setTimeout(()=>runAcceptanceInPlace(decodeURIComponent(acceptanceRequestedSeed)),80)}})}
