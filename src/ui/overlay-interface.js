;const overlayUiBaseDrawer=drawer,overlayUiBaseHud=hud;
function overlayButton(type,label){return`<button class="${overlayState().active===type?'active':''}" onclick="setOverlay('${type}')">${label}</button>`}
function overlayPanel(){let a=overlayState().active;return`<div class="section"><b>Realm overlays</b><p class="small">Only one map layer can be active. Overlays are visual-only and do not intercept world selection.</p><div class="grid">${overlayButton('none','None')}${overlayButton('territories','Territories')}${overlayButton('trade','Trade')}${overlayButton('prosperity','Prosperity')}${overlayButton('plague','Plague')}${overlayButton('unrest','Unrest')}${overlayButton('terrain','Terrain')}</div><div class="kv"><b>Active</b><span>${safeName(a)}</span><b>Visibility</b><span>Realm and regional altitude</span><b>Objects</b><span>${overlayRoot?.children.length||0}</span></div></div>`}
setDrawerTab=function(tab){productUiState().tab=['time','rates','acts','world','houses','overlays'].includes(tab)?tab:'time';drawer()};
drawer=function(){let panel=document.getElementById('drawer');if(productUiState().tab==='overlays'){let tabs=`<div class="ui-tabs">${productTabButton('time','Time')}${productTabButton('rates','Rates')}${productTabButton('acts','Acts')}${productTabButton('world','World')}${productTabButton('houses','Houses')}${productTabButton('overlays','Overlays')}</div>`;panel.innerHTML=`<b>ANNALS ${safeName(world.version)}</b>${tabs}${overlayPanel()}<p class="small">Hotkeys: Space pause · 1–4 speed · H hide UI · C Watch · Esc deselect.</p>`;return}overlayUiBaseDrawer();let tabs=panel.querySelector('.ui-tabs');if(tabs&&!tabs.querySelector('[data-overlay-tab]'))tabs.insertAdjacentHTML('beforeend',`<button data-overlay-tab class="" onclick="setDrawerTab('overlays')">Overlays</button>`)};
hud=function(){overlayUiBaseHud();let a=overlayState().active;if(a!=='none')meta.innerHTML+=`<span class="chip">Overlay ${safeName(a)}</span>`};
if(typeof document!=='undefined'){let style=document.createElement('style');style.textContent='#overlay-legend .small{margin-top:3px;color:#d7c7aa}.ui-tabs button.active,.grid button.active{box-shadow:inset 0 0 0 1px #d1aa3a;background:#3b3020;color:#f3dfad}';document.head.appendChild(style)}
if(typeof window!=='undefined'){window.setDrawerTab=setDrawerTab;window.setOverlay=setOverlay}