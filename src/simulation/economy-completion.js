;const ECONOMY_ACCEPTANCE_DAYS=18000,COG_LIMIT=2;
function economyCompletionState(){let s=economyState();s.harvestTaxCollected=Number.isFinite(s.harvestTaxCollected)?s.harvestTaxCollected:0;s.completedByMode=s.completedByMode||{road:0,cog:0};s.spending=s.spending||{armies:0,relief:0,bounties:0,rebuilding:0};s.disruptions=s.disruptions||{war:0,plague:0,bandit:0,drought:0};s.hungerCascade=s.hungerCascade||{mortality:0,migrated:0,events:0};s.conversion=s.conversion||{toolsProduced:0,oreConsumed:0,timberConsumed:0};s.nextHarvestTaxDay=Number.isFinite(s.nextHarvestTaxDay)?s.nextHarvestTaxDay:270;return s}
function economyPorts(){return world.settlements.filter(s=>s.site&&(s.site.harbour||s.site.waterAccess==='coast'))}
function coastOutward(){let side=terrainSpec?.coast?.side;return side==='west'?{x:-1,z:0}:side==='east'?{x:1,z:0}:side==='north'?{x:0,z:-1}:{x:0,z:1}}
function maritimePoints(from,to){let out=coastOutward(),d=Math.hypot(to.x-from.x,to.z-from.z),off=clamp(d*.28,220,760),c1={x:from.x+out.x*off,z:from.z+out.z*off},c2={x:to.x+out.x*off,z:to.z+out.z*off},pts=[];for(let i=0;i<=20;i++){let u=i/20,v=1-u,x=v*v*v*from.x+3*v*v*u*c1.x+3*v*u*u*c2.x+u*u*u*to.x,z=v*v*v*from.z+3*v*v*u*c1.z+3*v*u*u*c2.z+u*u*u*to.z;pts.push({x,y:10,z})}return pts}
function bestMaritimeTrade(){let ports=economyPorts(),best=null;if(ports.length<2)return null;for(const from of ports)for(const to of ports){if(from===to)continue;for(const good of ECONOMY_GOODS){let surplus=from.stores[good]-goodNeed(from,good)*20,deficit=goodNeed(to,good)*16-to.stores[good],spread=to.prices[good]-from.prices[good];if(surplus<=8||deficit<=8||spread<.18)continue;let distance=Math.hypot(to.x-from.x,to.z-from.z),score=Math.min(surplus,deficit)*spread/(1+distance/1800);if(!best||score>best.score)best={from,to,good,mode:'cog',score,spread,amount:clamp(Math.floor(Math.min(surplus,deficit,70+score)),10,180)}}return best}
const economyCompletionBaseBestTrade=bestTrade;
bestTrade=function(){let road=economyCompletionBaseBestTrade(),sea=bestMaritimeTrade();if(road){road.mode='road';road.spread=road.to.prices[road.good]-road.from.prices[road.good]}return sea&&(!road||sea.score>road.score*.92)?sea:road};
const economyCompletionBaseLaunchTrade=launchTrade;
launchTrade=function(candidate){if(!candidate)return null;if(candidate.mode!=='cog'){let t=economyCompletionBaseLaunchTrade(candidate);if(t){t.mode=t.mode||'road';t.departureSpread=Number.isFinite(candidate.spread)?candidate.spread:candidate.to.prices[candidate.good]-candidate.from.prices[candidate.good];t.departureValue=t.amount*t.departureSpread}return t}initThreats();if(candidate.from.disease?.quarantined||candidate.to.disease?.quarantined)return null;let state=economyCompletionState(),{from,to,good,amount}=candidate;from.stores[good]=clamp(from.stores[good]-amount,0,Math.max(300,from.pop*1.8));let points=maritimePoints(from,to),trade={id:`${world.seed}:trade${state.nextTradeId++}`,fromId:from.id,toId:to.id,good,amount,departDay:Math.floor(clock.day),arrivalDay:Math.floor(clock.day+Math.max(14,points.length*.85)),points,progress:0,status:'travelling',mode:'cog',departureSpread:candidate.spread,departureValue:amount*candidate.spread};state.trades.push(trade);if(state.trades.length>TRADE_LIMIT)state.trades.shift();from.economy.lastTradeDay=clock.day;return trade};
const economyCompletionBaseProductionFor=productionFor;
productionFor=function(s){let p=economyCompletionBaseProductionFor(s),reserveOre=goodNeed(s,'ore')*5,reserveTimber=goodNeed(s,'timber')*5,oreAvailable=Math.max(0,s.stores.ore-reserveOre),timberAvailable=Math.max(0,s.stores.timber-reserveTimber),capacity=s.type==='capital'?9:s.type==='town'?5:2,tools=Math.max(0,Math.min(capacity,oreAvailable/1.4,timberAvailable/2));p.tools=tools;s.economy.pendingConversion={ore:tools*1.4,timber:tools*2,tools};return p};
function settleConversionInputs(){let state=economyCompletionState();for(const s of world.settlements){let p=s.economy?.pendingConversion;if(!p)continue;s.stores.ore=clamp(s.stores.ore-p.ore,0,Math.max(300,s.pop*1.8));s.stores.timber=clamp(s.stores.timber-p.timber,0,Math.max(300,s.pop*1.8));state.conversion.toolsProduced+=p.tools;state.conversion.oreConsumed+=p.ore;state.conversion.timberConsumed+=p.timber;s.economy.pendingConversion=null}}
function applyHarvestTax(){let state=economyCompletionState(),rate=state.taxRate,total=0;for(const s of world.settlements){let grain=Math.min(s.stores.grain,(s.economy.produced.grain||0)*6*rate),fish=Math.min(s.stores.fish,(s.economy.produced.fish||0)*5*rate),value=grain*(s.prices.grain||1)+fish*(s.prices.fish||1);s.stores.grain=clamp(s.stores.grain-grain,0,Math.max(300,s.pop*1.8));s.stores.fish=clamp(s.stores.fish-fish,0,Math.max(300,s.pop*1.8));let revenue=value*.7;total+=revenue;let pressure=Math.max(0,rate-.08);s.unrest=clamp(s.unrest+pressure*24,0,100);let house=world.houses[s.owner];if(house&&house.id!==0)house.loyalty=clamp(house.loyalty-pressure*18+(rate<.06?.4:0),0,100)}state.treasury=clamp(state.treasury+total,0,100000);state.harvestTaxCollected+=total;if(total>0)eventChronicle('harvest-tax',6,world.settlements[0],`The autumn tithe brought ${Math.round(total)} crowns into the treasury; the levy followed the tax rate, and the great houses measured its burden against their loyalty.`,{class:'trade',kind:'harvest',participants:world.houses.map(h=>h.name),causes:[`harvest tax set at ${Math.round(rate*100)} percent`],consequences:['treasury increased','high rates raised unrest and loyalty pressure'],source:'economy-system'})}
function foodDaysAt(s){return(s.stores.grain+.8*s.stores.fish)/Math.max(1,goodNeed(s,'grain')+goodNeed(s,'fish'))}
function migrationDestination(from){let connected=world.roads.filter(r=>r.a===from.id||r.b===from.id).map(r=>world.settlements[r.a===from.id?r.b:r.a]).filter(Boolean),pool=connected.length?connected:world.settlements.filter(s=>s!==from);return pool.filter(s=>!s.disease?.quarantined).sort((a,b)=>foodDaysAt(b)-foodDaysAt(a)||a.unrest-b.unrest)[0]||null}
function hungerCascade(){if(Math.floor(clock.day)%15!==0)return;let state=economyCompletionState();for(const s of world.settlements){let days=foodDaysAt(s);if(days>=2||s.pop<=35)continue;let severity=clamp((2-days)/2,0,1),mortality=Math.min(s.pop-30,Math.max(0,s.pop*(.00035+.00055*severity))),migration=Math.min(s.pop-30-mortality,Math.max(0,s.pop*(.0008+.0014*severity))),to=migrationDestination(s);s.pop=clamp(s.pop-mortality-migration,30,9000);if(to)to.pop=clamp(to.pop+migration,30,9000);state.hungerCascade.mortality+=mortality;state.hungerCascade.migrated+=migration;s.unrest=clamp(s.unrest+severity*.8,0,100);if(clock.day-(s.economy.lastCascadeDay||-999)>240&&severity>.45){s.economy.lastCascadeDay=clock.day;state.hungerCascade.events++;eventChronicle('hunger-cascade',9,s,`${s.name} exhausted its food stores; hunger drove ${Math.max(1,Math.round(migration))} people toward ${to?.name||'the roads'} and added ${Math.max(1,Math.round(mortality))} names to the mortality roll.`,{class:'trade',kind:'famine',participants:[s.name,to?.name].filter(Boolean),causes:['food stores fell below two days of need'],consequences:['population declined','migration redistributed households','unrest increased'],source:'economy-system'})}}}
function armyUpkeep(){let state=economyCompletionState(),armies=warState().armies.filter(a=>['marching','besieging','victorious'].includes(a.status));for(const a of armies){let cost=.35+a.strength*.0015,home=world.settlements[a.fromId],grain=Math.min(home?.stores.grain||0,.25+a.strength*.0007),tools=Math.min(home?.stores.tools||0,.02+a.strength*.00004),paid=Math.min(state.treasury,cost);state.treasury=clamp(state.treasury-paid,0,100000);state.spending.armies+=paid;if(home){home.stores.grain=clamp(home.stores.grain-grain,0,Math.max(300,home.pop*1.8));home.stores.tools=clamp(home.stores.tools-tools,0,Math.max(300,home.pop*1.8))}if(paid<cost*.75){a.supply=clamp(a.supply-.45,0,200);a.morale=clamp(a.morale-.18,0,100)}}}
function disruptWarTrade(){let state=economyCompletionState(),active=warState().campaigns.filter(c=>c.status!=='ended');if(!active.length)return;for(const t of state.trades.filter(t=>t.status==='travelling'&&!t.warChecked)){let from=world.settlements[t.fromId],to=world.settlements[t.toId],campaign=active.find(c=>c.targetSettlementId===from.id||c.targetSettlementId===to.id||[c.attackerHouseId,c.defenderHouseId].includes(from.owner)||[c.attackerHouseId,c.defenderHouseId].includes(to.owner));if(!campaign)continue;t.warChecked=true;let lost=Math.max(1,Math.floor(t.amount*.12));t.amount=Math.max(1,t.amount-lost);t.arrivalDay+=10;state.disruptions.war++;if(clock.day-(state.lastWarTradeEntry||-999)>180){state.lastWarTradeEntry=clock.day;eventChronicle('war-trade-disruption',8,to,`${campaign.name} delayed a ${t.mode==='cog'?'coastal cog':'road caravan'} and consumed ${lost} measures of ${t.good}; war funding reached beyond the battlefield into stores and prices.`,{class:'war',kind:'war',participants:[campaign.id,from.name,to.name],causes:['an active campaign crossed the trade network'],consequences:['cargo was lost','delivery was delayed','scarcity pressure increased'],source:'economy-system'})}}}
function recordExistingDisruptions(){let state=economyCompletionState();for(const t of state.trades){if(t.banditChecked&&!t.economyBanditCounted){t.economyBanditCounted=true;state.disruptions.bandit++}}for(const s of world.settlements){if(s.disease?.quarantined&&!s.economy.quarantineCounted){s.economy.quarantineCounted=true;state.disruptions.plague++}if(!s.disease?.quarantined)s.economy.quarantineCounted=false}if(world.drought&&!state.droughtCounted){state.droughtCounted=true;state.disruptions.drought++}if(!world.drought)state.droughtCounted=false}
const economyCompletionBaseCompleteTrades=completeTrades;
completeTrades=function(){let before=new Set(economyCompletionState().trades.filter(t=>t.status==='arrived').map(t=>t.id));economyCompletionBaseCompleteTrades();let state=economyCompletionState();for(const t of state.trades)if(t.status==='arrived'&&!before.has(t.id)&&!t.economyCompletionCounted){t.economyCompletionCounted=true;state.completedByMode[t.mode||'road']=(state.completedByMode[t.mode||'road']||0)+1}};
const economyCompletionBaseEconomyDay=economyDay;
economyDay=function(){economyCompletionBaseEconomyDay();settleConversionInputs();let state=economyCompletionState();if(clock.day>=state.nextHarvestTaxDay){applyHarvestTax();state.nextHarvestTaxDay+=360}if(Math.floor(clock.day)%90===0&&state.trades.filter(t=>t.status==='travelling'&&t.mode==='cog').length<COG_LIMIT)launchTrade(bestMaritimeTrade());hungerCascade()};
const economyCompletionBaseRebuildTick=rebuildTick;
rebuildTick=function(s){let before=new Set((s.layout?.lots||[]).filter(l=>l.building?.state==='rebuilding').map(l=>l.id));economyCompletionBaseRebuildTick(s);let state=economyCompletionState();for(const lot of(s.layout?.lots||[]))if(lot.building?.state==='rebuilding'&&!before.has(lot.id)&&!lot.building.economyPaid){let cost=lot.building.tier==='stone'?55:lot.building.tier==='timber'?28:14,paid=Math.min(state.treasury,cost);state.treasury=clamp(state.treasury-paid,0,100000);state.spending.rebuilding+=paid;lot.building.economyPaid=true;if(paid<cost)s.prosperity=clamp(s.prosperity-(cost-paid)*.08,0,100)}};
const economyCompletionBaseAct=act;
act=function(a){let state=economyCompletionState(),before=state.treasury;economyCompletionBaseAct(a);if(a==='grain')state.spending.relief+=Math.max(0,before-state.treasury)};
const economyCompletionBasePatrolBandits=patrolBandits;
patrolBandits=function(){let state=economyCompletionState(),before=state.treasury;economyCompletionBasePatrolBandits();state.spending.bounties+=Math.max(0,before-state.treasury)};
const economyCompletionBaseTick=tick;
tick=function(){economyCompletionBaseTick();armyUpkeep();disruptWarTrade();recordExistingDisruptions()};
const economyCompletionBaseSummary=economySummary;
economySummary=function(){let base=economyCompletionBaseSummary(),state=economyCompletionState();return{...base,ports:economyPorts().map(s=>({id:s.id,name:s.name})),completedByMode:{...state.completedByMode},harvestTaxCollected:Math.round(state.harvestTaxCollected*100)/100,spending:Object.fromEntries(Object.entries(state.spending).map(([k,v])=>[k,Math.round(v*100)/100])),disruptions:{...state.disruptions},hungerCascade:{mortality:Math.round(state.hungerCascade.mortality),migrated:Math.round(state.hungerCascade.migrated),events:state.hungerCascade.events},conversion:{toolsProduced:Math.round(state.conversion.toolsProduced),oreConsumed:Math.round(state.conversion.oreConsumed),timberConsumed:Math.round(state.conversion.timberConsumed)},tradesByMode:state.trades.reduce((a,t)=>(a[t.mode||'road']=(a[t.mode||'road']||0)+1,a),{}),highValueArrivals:state.trades.filter(t=>t.status==='arrived'&&(t.departureSpread||0)>=.18).length}};
function simulateEconomyCase(seed,days=ECONOMY_ACCEPTANCE_DAYS,taxRate=.08){clock={day:1,speed:8,acc:0};director={queue:[],until:0,suspend:0};world=makeWorld(String(seed));world.headless=true;firstEntries();initEconomy();initThreats();economyState().taxRate=taxRate;threatState().myth='off';for(let i=0;i<days;i++){tick();director.queue.length=0}let summary=economySummary(),finite=[summary.treasury,summary.taxCollected,summary.harvestTaxCollected,...summary.settlements.flatMap(s=>[s.hunger,s.unrest,...Object.values(s.stores),...Object.values(s.prices)])].every(Number.isFinite),storesBounded=world.settlements.every(s=>ECONOMY_GOODS.every(g=>s.stores[g]>=0&&s.stores[g]<=Math.max(300,s.pop*1.8)+.01)),pricesBounded=world.settlements.every(s=>ECONOMY_GOODS.every(g=>s.prices[g]>=.35&&s.prices[g]<=3.5)),populationBounded=world.settlements.every(s=>s.pop>=30&&s.pop<=9000),registryBounded=economyState().trades.length<=TRADE_LIMIT,disrupted=['war','plague','bandit','drought'].every(k=>summary.disruptions[k]>0),foodConsequences=summary.hungerCascade.events>0&&summary.hungerCascade.mortality>0&&summary.hungerCascade.migrated>0,conversion=summary.conversion.toolsProduced>0&&summary.conversion.oreConsumed>0&&summary.conversion.timberConsumed>0,cogRequired=summary.ports.length>=2,cogs=!cogRequired||summary.completedByMode.cog>0;return{seed:String(seed),days,passed:finite&&storesBounded&&pricesBounded&&populationBounded&&registryBounded&&disrupted&&foodConsequences&&conversion&&cogs,checks:{finite,storesBounded,pricesBounded,populationBounded,registryBounded,disrupted,foodConsequences,conversion,cogs},summary}}
function economyTaxProbe(seed,rate){let r=simulateEconomyCase(seed,720,rate);return{rate,treasury:r.summary.treasury,loyalty:Math.round(world.houses.slice(1).reduce((n,h)=>n+h.loyalty,0)/Math.max(1,world.houses.length-1)*100)/100,unrest:Math.round(world.settlements.reduce((n,s)=>n+s.unrest,0)/world.settlements.length*100)/100,taxCollected:Math.round((r.summary.taxCollected+r.summary.harvestTaxCollected)*100)/100}}
function runEconomyAcceptance(seed,days=ECONOMY_ACCEPTANCE_DAYS){let main=simulateEconomyCase(seed,days,.08),low=economyTaxProbe(seed,.03),high=economyTaxProbe(seed,.22),taxEffects=high.taxCollected>low.taxCollected&&high.loyalty<low.loyalty&&high.unrest>low.unrest;return{seed:String(seed),days,passed:main.passed&&taxEffects,checks:{...main.checks,taxEffects},summary:main.summary,taxProbe:{low,high}}}
if(typeof window!=='undefined'){window.ANNALS_DEBUG.runEconomyAcceptance=runEconomyAcceptance;window.ANNALS_DEBUG.economyCompletion=()=>economySummary();window.ANNALS_DEBUG.economyAcceptanceDays=ECONOMY_ACCEPTANCE_DAYS}