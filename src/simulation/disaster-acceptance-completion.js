;function plagueTradeAcceptanceProbe(seed){resetDisasterWorld(seed,'off');seedPlague();let source=world.settlements.find(s=>(s.disease?.infected||0)>0),road=world.roads.find(r=>r.a===source?.id||r.b===source?.id),target=road?world.settlements[road.a===source.id?road.b:road.a]:world.settlements.find(s=>s!==source);if(!source||!target)return{spread:false,quarantined:false,sourceId:source?.id??null,targetId:target?.id??null};source.disease.infected=Math.max(source.disease.infected,source.pop*0.3);source.disease.susceptible=Math.max(0,source.pop-source.disease.infected);for(let i=0;i<160&&!target.disease.quarantined;i++){plagueFromTrade({id:`acceptance-plague-${i}`,fromId:source.id,toId:target.id,amount:300,status:'arrived'});updatePlague()}return{spread:target.disease.infected>0,quarantined:target.disease.quarantined,sourceId:source.id,targetId:target.id,infected:Math.round(target.disease.infected),rate:Math.round(target.disease.infected/Math.max(1,target.pop)*10000)/10000}}
const disasterAcceptanceBaseRun=runDisasterAcceptance;
runDisasterAcceptance=function(seed){let plague=plagueTradeAcceptanceProbe(seed),result=disasterAcceptanceBaseRun(seed);result.plague=plague;result.checks.plagueTradeQuarantine=plague.spread&&plague.quarantined;result.passed=Object.values(result.checks).every(Boolean);return result};
if(typeof window!=='undefined')window.ANNALS_DEBUG.runDisasterAcceptance=runDisasterAcceptance;
