const CHRONICLE_LIMIT=240;
function chronicleStructuralSummary(){let entries=world.events.map(e=>({day:e.day,class:e.class,kind:e.kind||e.class,header:!!e.header,eventId:e.eventId||null,memoryId:e.memoryId||null,scarId:e.scarId||null,location:e.location||null,pos:{x:Math.round(e.pos?.x||0),y:Math.round(e.pos?.y||0),z:Math.round(e.pos?.z||0)}}));return{count:entries.length,limit:CHRONICLE_LIMIT,classes:entries.reduce((a,e)=>(a[e.class]=(a[e.class]||0)+1,a),{}),headers:entries.filter(e=>e.header).length,references:entries.filter(e=>e.memoryId).length,targetable:entries.filter(e=>Number.isFinite(e.pos.x)&&Number.isFinite(e.pos.y)&&Number.isFinite(e.pos.z)).length,entries}}
function chronicleRegressionRun(seed,days=1800,opt={initialChronicle:true,fireAt:20,droughtAt:120,festivalAt:240,grainAt:360}){let result=runHeadless(seed,days,opt),summary=chronicleStructuralSummary();return{seed,days,summary,reigns:result.reigns||[],memories:result.memories||[],events:result.structuredEvents||[],exportText:formatChronicleExport()}}
if(typeof window!=='undefined'){window.ANNALS_DEBUG.chronicleSummary=chronicleStructuralSummary;window.ANNALS_DEBUG.chronicleRegressionRun=chronicleRegressionRun;window.ANNALS_DEBUG.formatChronicleExport=()=>formatChronicleExport();window.ANNALS_DEBUG.chronicleLimit=CHRONICLE_LIMIT}
